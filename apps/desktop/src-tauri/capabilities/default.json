{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allows the main window to access core IPC commands and the system dialog picker.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
